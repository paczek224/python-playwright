### Run the app
From [app](app) directory:
- Run `npm i` to install all the project dependencies.
- Run `npm start` to start the dev server.

### Run Tests
From [tests](system_tests) directory:
- Run `pytest -n 3`
